# Remotion (Video Templates)

This folder contains a standalone Remotion project for generating short videos
and still images using React components.

Setup

```bash
cd remotion
npm install
```

Preview

```bash
npm run studio
```

Render

```bash
npm run render
```

Still image

```bash
npm run render:still
```
